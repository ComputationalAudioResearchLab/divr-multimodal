# Model storage

This folder is reserved for local model artifacts used by `divr_llm`.

The default Qwen2Audio LoRA config writes cache files to `model/cache/qwen2audio` and saves adapters to `model/qwen2audio_lora`.

Keep downloaded checkpoints, merged weights, and experiment outputs inside this folder so the project stays self-contained.